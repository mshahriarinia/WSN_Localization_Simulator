function d1 = d(i, j)
%gives the euclidean distance of the two nodes specified by the indexes
%i=1;j=2;zeros(1,2);
%i = uint16(round(i));
%j = uint16(j);
% xi = X(i);
% xj  = X(j);

d1 = (X(i) - X(j)) ^ 2 + (Y(i) - Y(j)) ^ 2;